name: memory_circle
description: Omoide no Wa MVP Flutter app.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.3
  file_picker: ^8.1.4
  http: ^1.2.2
  http_parser: ^4.0.2
  share_plus: ^10.1.0
  shared_preferences: ^2.3.0
  firebase_core: ^4.11.0
  firebase_messaging: ^16.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
