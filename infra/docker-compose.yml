services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: memory_circle
      POSTGRES_PASSWORD: memory_circle
      POSTGRES_DB: memory_circle
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
